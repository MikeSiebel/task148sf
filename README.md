🚀 # task148sf_DataTypePHP

Практическая работа для модуля "Сессии и Cookie" 

## Используемые технологии

* `PHP`

* `Open Server Panel`


Практическая работа по теме "Сессии и Cookie. Демо-версия сайта для SPA-салона. Личный кабинет"

Шаги выполнения

1. "Организуйте хранение паролей и логинов ваших пользователей в отдельном файле. Пароли должны храниться в зашифрованном виде."
    - для хранения  логинов и паролей в хэшированом виде будем использовать  файл `usersDB.php`;
    - в качестве примера захешируем следующие пароли: 
        1) `['login' => 'admin', 'password' => 'P@ssw0rd']`; 
        2) `['login' => 'moderator', 'password' => 'password']`; 
        3) `['login' => 'user', 'password' => '123']`,
    - Чтобы создать хэши пароля из пароля,  используем `password_hash`-функцию во вспомогательном файле `pwd_hash.php`:
        `$hash = password_hash($password, PASSWORD_BCRYPT)`; 
    - при аутентификации используется функция `password_verify($password, $user['hash'])`;  

2. "Создадим несколько полезных функций и выделим их в отдельный файл" `fstorage.php`:
    - созданы функции `getUsersList()`, `existsUser($login)`, `checkPassword($login, $password)`, `getCurrentUser()`;

3. "Сделайте возможность аутентификации пользователя":
    - выполнено

4. "Разместите на главной странице индивидуальную акцию. ":
    - выполнено

5. "Сделайте возможность выхода из личного кабинета.":
    - выполнено

6. "Спросите дату рождения пользователя":
    - не выполнено


---